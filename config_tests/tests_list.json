[
    {
    "name": "redis_video",
    "bench_template": "python3 experiment/migration/redis/bench_video_cache_realistic.py --threads {threads} --payload-mode {payload_mode} --framerate {framerate} --duration {duration} --frame-width {frame_width} --frame-height {frame_height} --objects-per-frame {objects_per_frame} --analysis-intensity {analysis_intensity} --inference-model {inference_model}",
    "resolution": "1920x1080,1280x720",
    "payload_mode": "binary",
    "analysis_intensity": "mechanical,comprehensive",
    "inference_model": "ssd_mobile,yolov5_medium",
    "objects_per_frame": "4,8",
    "duration": 100,
    "threads": 4,
    "framerate": "30,60",
    "runs": 2,
    "container": "redis",
    "output": "results/redis_video.csv",
    "log_dir": "logs/redis_video"
  },
  {
    "name": "redis_sensoragg",
    "bench_template": "python3 experiment/migration/redis/bench_sensoragg.py --threads {threads} --payload-mode {payload_mode} --rps {rps} --duration {duration} --payload-size {payload} --set-key sensors:ts_test",
    "rps": "40,660",
    "payload_size": "384B,2200B",
    "payload_mode": "json",
    "duration": 100,
    "threads": 4,
    "runs": 2,
    "container": "redis",
    "output": "results/redis_sensoragg.csv",
    "log_dir": "logs/redis_sensoragg",
    "checkpoint_file_locks": false
  },
  {
    "name": "influx_sensoragg",
    "bench_template": "python3 experiment/migration/influxdb/bench_sensoragg.py --threads {threads} --payload-mode {payload_mode} --rps {rps} --duration {duration} --payload-size {payload} --size-distribution {size_distribution}",
    "rps": "40,690",
    "payload_size": "256B,2000B",
    "payload_mode": "json,binary",
    "size_distribution": "uniform,normal,zipf",
    "sensors_per_device": "5,10",
    "duration": 100,
    "threads": 4,
    "runs": 2,
    "container": "influxdb",
    "output": "results/influx_sensoragg.csv",
    "log_dir": "logs/influx_sensoragg"
  },
  {
    "name": "redis_cartelem",
    "bench_template": "python3 experiment/migration/redis/bench_cartelem.py --threads {threads} --payload-mode json --rps {rps} --duration {duration} --payload-size {payload}",
    "rps": "50,800",
    "payload_size": "512B,3500B",
    "payload_mode": "json,binary",
    "size_distribution": "uniform,normal,zipf",
    "duration": 100,
    "threads": 4,
    "runs": 2,
    "container": "redis",
    "output": "results/redis_cartelem.csv",
    "log_dir": "logs/redis_cartelem"
  },
  {
    "name": "influx_cartelem",
    "bench_template": "python3 experiment/migration/influxdb/bench_cartelem.py --threads {threads} --payload-mode {payload_mode} --rps {rps} --duration {duration} --payload-size {payload} --size-distribution {size_distribution} --vehicle-pattern normal_city",
    "rps": "50,760",
    "payload_size": "768B,3600B",
    "payload_mode": "json,binary",
    "size_distribution": "uniform,normal,zipf",
    "duration": 100,
    "threads": 4,
    "runs": 2,
    "container": "influxdb",
    "output": "results/influx_cartelem.csv",
    "log_dir": "logs/influx_cartelem"
  },
  {
    "name": "elasticsearch",
    "bench_template": "python3 experiment/migration/elasticsearch/benchmark.py --rps {rps} --duration {duration} --payload-size {payload} --threads {threads}",
    "rps": "50,190",
    "payload_size": "256B,2KB",
    "duration": 90,
    "threads": 2,
    "runs": 2,
    "container": "elasticsearch",
    "checkpoint_file_locks": true,
    "output": "results/elasticsearch.csv",
    "log_dir": "logs/elasticsearch"
  }
]
