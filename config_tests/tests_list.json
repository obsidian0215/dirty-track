[
  {
    "name": "redis_video",
    "bench_template": "python3 experiment/migration/redis/bench_video_cache.py --threads {threads} --payload-mode {payload_mode} --framerate {framerate} --duration {duration} --frame-width {frame_width} --frame-height {frame_height} --objects-per-frame {objects_per_frame} --analysis-intensity {analysis_intensity} --inference-model {inference_model} --pacing --payload-mixture",
    "resolution": "1920x1080",
    "payload_mode": "binary",
    "analysis_intensity": "comprehensive",
    "inference_model": "yolov5_medium",
    "objects_per_frame": "8",
    "duration": 90,
    "threads": 4,
    "framerate": "24,70",
    "runs": 2,
    "container": "redis",
    "output": "results/redis_video.csv",
    "log_dir": "logs/redis_video"
  },
  {
    "name": "redis_sensoragg",
    "bench_template": "python3 experiment/migration/redis/bench_sensoragg.py --threads {threads} --payload-mode {payload_mode} --rps {rps} --duration {duration} --payload-size {payload} --set-key sensors:ts_test --pacing poisson --payload-mixture \"70:50-200,25:512-1024,5:3500-8000\" --device-count 1000",
    "rps": "40,660",
    "payload_size": "384B,2200B",
    "payload_mode": "json",
    "duration": 90,
    "threads": 4,
    "runs": 2,
    "container": "redis",
    "output": "results/redis_sensoragg.csv",
    "log_dir": "logs/redis_sensoragg",
    "checkpoint_file_locks": false
  },
  {
    "name": "influx_sensoragg",
    "bench_template": "python3 experiment/migration/influxdb/bench_sensoragg.py --threads {threads} --payload-mode {payload_mode} --rps {rps} --duration {duration} --payload-size {payload} --size-distribution {size_distribution} --pacing --payload-mixture --device-count 1000",
    "rps": "40,690",
    "payload_size": "256B,2000B",
    "payload_mode": "json",
    "size_distribution": "uniform",
    "sensors_per_device": "10",
    "duration": 90,
    "threads": 4,
    "runs": 2,
    "container": "influxdb",
    "output": "results/influx_sensoragg.csv",
    "log_dir": "logs/influx_sensoragg"
  },
  {
    "name": "redis_cartelem",
    "bench_template": "python3 experiment/migration/redis/bench_cartelem.py --threads {threads} --payload-mode json --rps {rps} --duration {duration} --payload-size {payload} --pacing poisson --payload-mixture \"70:50-200,25:512-1024,5:3500-8000\" --device-count 1000",
    "rps": "50,649",
    "payload_size": "512B,3500B",
    "payload_mode": "json",
    "size_distribution": "uniform",
    "duration": 90,
    "threads": 4,
    "runs": 2,
    "container": "redis",
    "output": "results/redis_cartelem.csv",
    "log_dir": "logs/redis_cartelem"
  },
  {
    "name": "influx_cartelem",
    "bench_template": "python3 experiment/migration/influxdb/bench_cartelem.py --threads {threads} --payload-mode {payload_mode} --rps {rps} --duration {duration} --payload-size {payload} --size-distribution {size_distribution} --vehicle-pattern normal_city --pacing --payload-mixture --device-count 1000",
    "rps": "40,760",
    "payload_size": "768B,3600B",
    "payload_mode": "json,binary",
    "size_distribution": "uniform",
    "duration": 90,
    "threads": 4,
    "runs": 2,
    "container": "influxdb",
    "output": "results/influx_cartelem.csv",
    "log_dir": "logs/influx_cartelem"
  },
  {
    "name": "elasticsearch",
    "bench_template": "python3 experiment/migration/elasticsearch/bench.py --rps {rps} --duration {duration} --payload-size {payload} --threads {threads}",
    "rps": "50,342",
    "payload_size": "256B,2KB",
    "duration": 90,
    "threads": 2,
    "runs": 2,
    "container": "elasticsearch",
    "checkpoint_file_locks": true,
    "output": "results/elasticsearch.csv",
    "log_dir": "logs/elasticsearch"
  }
]
